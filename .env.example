# Copy to .env and fill in as needed. Never commit .env.
#
# Nothing here is required for the current milestone (ingestion/retrieval/evaluation
# run fully offline with local models). These become relevant at the
# grounded-generation milestone.

# --- Grounded answer generation (added at the generation milestone) ---
# ANTHROPIC_API_KEY=

# --- Local model cache locations (optional) ---
# HF_HOME=./.cache/huggingface
# SENTENCE_TRANSFORMERS_HOME=./.cache/sentence-transformers

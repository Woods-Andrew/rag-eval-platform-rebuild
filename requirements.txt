# Runtime + development dependencies for the current milestone.
# Dense-retrieval dependencies (sentence-transformers, torch) are added at the
# dense retrieval milestone, not before.

pymupdf==1.28.2
pytest==9.1.1
rank-bm25==0.2.2
numpy==2.4.6

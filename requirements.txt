# Runtime + development dependencies for the current milestone.
#
# The unit suite never loads a model: encoders and rerankers are injected, and
# sentence-transformers is imported lazily inside its adapter. These are needed to
# *run* retrieval on a real corpus, not to run the tests.

pymupdf==1.28.0
pytest==9.1.1
rank-bm25==0.2.2
numpy==2.4.6
sentence-transformers==5.6.0
torch==2.13.0
streamlit==1.59.0
